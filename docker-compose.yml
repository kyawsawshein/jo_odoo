version: "3.9"

services:
  db:
    image: postgres:17
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - ./data-db/db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 5s
      retries: 5
    restart: always
    networks:
      - odoo19-net

  odoo19:
    build: .
    depends_on:
      db:
        condition: service_healthy   # ✅ Wait for Postgres to be ready
    ports:
      - "8090:8090"
    volumes:
      - ./extra_addons:/opt/odoo/extra_addons
      - ./vendor_addons:/opt/odoo/vendor_addons
      - ./config/odoo.conf:/etc/odoo/odoo.conf
      - ./odoo-data:/opt/odoo/data
      - ./odoo-log:/var/log/odoo
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=${POSTGRES_USER}
      - DB_PASSWORD=${POSTGRES_PASSWORD}
    command: >
      python odoo/odoo-bin -c /etc/odoo/odoo.conf -d odoo
      -i api_module
      -u zervi_account_asset
    restart: always
    networks:
      - odoo19-net

volumes:
  odoo-data:

networks:
  odoo19-net:
    driver: bridge
